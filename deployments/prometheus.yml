global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'smart-emotion-journal-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:8080', 'host.docker.internal:8080']
