apiVersion: v1
kind: Service
metadata:
  name: smart-emotion-journal-backend-service
  labels:
    app: smart-emotion-journal-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: smart-emotion-journal-backend
